[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tracenox"
version = "0.1.0"
description = "Evidence-Driven Cybersecurity Investigation Toolkit"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["tracenox*"]
